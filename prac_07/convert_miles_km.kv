BoxLayout:
    orientation: 'vertical'
    BoxLayout:
        orientation: 'horizontal'
        TextInput:
            id: input_miles
            multiline: False
            text: ""
            on_text: app.handle_conversion()
#            on_text: app.handle_conversion(input_miles.text)  # Only if get_validated_miles function is not used.
        BoxLayout:
            orientation: 'vertical'
            size_hint_x: 0.25
            Button:
                text: "Up"
                on_press: app.handle_increment(1)
            Button:
                text: "Down"
                on_press: app.handle_increment(-1)
#    Button:
#        text: "Convert miles to km"
#        on_press: app.handle_conversion()
#        on_press: app.handle_conversion(input_miles.text)  # Only if get_validated_miles function is not used.
    Label:
        id: output_label
#        text: ""
        text: app.result_in_km
        font_size: 48
        color: (1, 1, 0, 1)
